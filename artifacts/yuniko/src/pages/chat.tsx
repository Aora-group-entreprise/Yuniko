import {useEffect,useRef,useState} from "react";import {useLocation,useParams} from "wouter";import {ArrowLeft,Phone,Video,MoreHorizontal,Send,Mic} from "lucide-react";import {apiJson} from "@/lib/api";import {useAuth} from "@/lib/auth-context";
type User={id:number;username:string;displayName:string;avatarUrl:string|null};type Msg={id:number;senderId:number;body:string;mediaUrl:string|null;kind:string;createdAt:string};type Conv={id:number;userId:number;username:string;displayName:string;avatarUrl:string|null;lastMessage:string};
export default function Chat(){const [,setLocation]=useLocation();const params=useParams<{userId:string}>();const targetId=Number(params?.userId);const {user:me}=useAuth();const [user,setUser]=useState<User|null>(null);const [conv,setConv]=useState<Conv|null>(null);const [messages,setMessages]=useState<Msg[]>([]);const [text,setText]=useState("");const bottom=useRef<HTMLDivElement>(null);
 useEffect(()=>{if(!targetId)return;apiJson<{user:User}>(`/users/${targetId}`).then(d=>setUser(d.user)).catch(()=>{});apiJson<{conversations:Conv[]}>("/conversations").then(d=>{const c=d.conversations.find(x=>x.userId===targetId);if(c)setConv(c);else apiJson<{conversation:{id:number}}>("/conversations",{method:"POST",body:JSON.stringify({userId:targetId})}).then(x=>setConv({id:x.conversation.id,userId:targetId,username:"",displayName:"",avatarUrl:null,lastMessage:""})).catch(()=>{});}).catch(()=>{});},[targetId]);
 useEffect(()=>{if(!conv)return;let active=true;const load=async()=>{const d=await apiJson<{messages:Msg[]}>(`/conversations/${conv.id}/messages`).catch(()=>({messages:[]}));if(active)setMessages(d.messages);};load();const timer=window.setInterval(load,2500);return()=>{active=false;window.clearInterval(timer)}},[conv?.id]);useEffect(()=>bottom.current?.scrollIntoView({behavior:"smooth"}),[messages]);
 const send=async()=>{const body=text.trim();if(!body||!conv||!me)return;const d=await apiJson<{message:Msg}>(`/conversations/${conv.id}/messages`,{method:"POST",body:JSON.stringify({body})}).catch(()=>null);if(d?.message)setMessages(m=>[...m,d.message]);setText("");};
 const av=user?.avatarUrl??`https://api.dicebear.com/8.x/initials/svg?seed=${encodeURIComponent(user?.displayName??"U")}&backgroundColor=FF006E`;
 return <div className="w-full max-w-[430px] mx-auto min-h-screen bg-background flex flex-col"><header className="sticky top-0 z-40 px-4 py-3 flex items-center gap-3 bg-black/95 border-b border-white/5"><button onClick={()=>setLocation("/messages")}><ArrowLeft size={22} className="text-white/80"/></button><img src={av} className="w-9 h-9 rounded-full object-cover"/><div className="flex-1"><b className="text-white text-sm">{user?.displayName??"User"}</b><p className="text-white/40 text-xs">@{user?.username??""}</p></div><button onClick={()=>setLocation(`/voice-call/${targetId}`)}><Phone size={20} className="text-white/70"/></button><button onClick={()=>setLocation(`/video-call/${targetId}`)}><Video size={20} className="text-white/70"/></button><MoreHorizontal size={20} className="text-white/70"/></header><div className="flex-1 overflow-y-auto px-3 py-4 pb-24">{messages.map(m=>{const mine=m.senderId===me?.id;return <div key={m.id} className={`flex mb-2 ${mine?"justify-end":"justify-start"}`}><div className={`max-w-[75%] px-3.5 py-2.5 rounded-2xl text-sm ${mine?"rounded-br-sm text-white bg-gradient-to-r from-pink-500 to-purple-600":"rounded-bl-sm text-white/90 bg-white/[.08]"}`}>{m.body}</div></div>})}<div ref={bottom}/></div><div className="fixed bottom-0 left-1/2 -translate-x-1/2 w-full max-w-[430px] px-3 py-3 bg-black/95 border-t border-white/5"><div className="flex gap-2 items-center"><Mic size={21} className="text-pink-400"/><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>e.key==="Enter"&&send()} placeholder="Message..." className="flex-1 bg-white/[.07] rounded-full px-4 py-3 text-white outline-none"/><button onClick={send} className="w-10 h-10 rounded-full flex items-center justify-center bg-gradient-to-r from-pink-500 to-purple-600"><Send size={16} className="text-white"/></button></div></div></div>}
